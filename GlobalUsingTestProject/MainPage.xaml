<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GlobalUsingTestProject.MainPage">

    <ScrollView>
        <VerticalStackLayout Padding="30,0"
                             Spacing="25">
            <Label Text="{Binding Count, StringFormat='Models Added: {0}'}" />
            <Button Text="Add New"
                    Command="{Binding AddNewCommand}"
                    HorizontalOptions="Fill" />

            <Button Text="Go to Display"
                    Command="{Binding GoToDisplayPageCommand}"
                    HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
